/**
 * Versioned independently from `domain/diagnostic`'s `FORMULA_VERSION` —
 * the tracking engine computes from a continuous ledger, not a one-time
 * survey, and the two evolve on separate schedules.
 */
export const TRACKING_FORMULA_VERSION = "1.0.0";
